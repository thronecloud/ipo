yfinance>=0.2.36
pandas>=2.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0

# Living-engine: durable store + migrations + config + scheduler
SQLAlchemy>=2.0.30
psycopg[binary]>=3.1.18
alembic>=1.13.0
python-dotenv>=1.0.0
APScheduler>=3.10.4

# API read-layer (FastAPI dashboards)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# AMFI xlsx parsing (universe classification)
openpyxl>=3.1.0

# Document extraction: native PDF text (PyMuPDF), ruled-table parsing (pdfplumber),
# and OCR for scanned pages (pytesseract -> the tesseract-ocr system binary). Pages are
# rasterized in-memory by PyMuPDF, so no poppler/pdf2image is needed.
pymupdf>=1.24.0
pdfplumber>=0.11.0
pytesseract>=0.3.10
Pillow>=10.0.0

# LLM analysis output contract enforcement
jsonschema>=4.0.0

# Testing
pytest>=8.0.0
httpx>=0.27.0
